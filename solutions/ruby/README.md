# Ruby Solutions

## Running Locally

Tests are written in [RSpec](https://rspec.info/). You can run tests using the
following command:

```shell
rspec <spec filename>
```

for example:

```shell
rspec contamination_spec.rb
```

